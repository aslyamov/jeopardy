# Своя Игра

Браузерная викторина «Своя Игра» для компании друзей. Работает полностью статически — без сервера, прямо с локального файла или GitHub Pages.

## Возможности

**Игровой процесс**
- Игровое поле с категориями и стоимостью вопросов
- Одновременный выбор нескольких игроков для начисления очков
- Таймер на вопрос (опционально, настраивается в секундах)
- Блокировка кнопок клавишей `L` / `Д` (работает в обеих раскладках)
- Автоматический переход к итогам после последнего вопроса
- Сохранение прогресса в localStorage после каждого хода
- Возобновление незавершённых игр через «Продолжить» на главной

**Вопросы**
- Текстовые вопросы и ответы
- Картинки по URL (только `http`/`https`, защита от `javascript:`)
- Шахматные позиции по FEN (рендер через Chessground)

**Редактор наборов**
- Визуальный редактор: категории, вопросы, ответы, картинки, FEN
- Валидация FEN через chess.js
- Перемещение вопросов вверх/вниз (▲/▼) и дублирование (⧉)
- Сохранение наборов в localStorage
- Экспорт набора в JSON-файл, импорт из JSON-файла
- Переход «Играть» сразу из редактора

**Итоги**
- Рейтинг игроков с медалями 👑 🥈 🥉
- Кнопки «Сыграть снова» и «На главную»

## Как добавить встроенный набор

1. Положи `.json` файл в папку `packs/` (структура — как у `mittelsphiel.json`)
2. Запусти `npm run build`

Файлы из `packs/` подхватываются автоматически — никакого кода менять не нужно.

## Структура проекта

```
packs/                  ← встроенные наборы вопросов (JSON)
src/
  main.ts               ← точка входа
  router.ts             ← navigate(screen) → рендер экрана
  state.ts              ← глобальное состояние (packs, game, …)
  storage.ts            ← localStorage: сохранения игр + пользовательские паки
  types.ts              ← интерфейсы Pack, Question, GameState, …
  data.ts               ← ре-экспорт BUILTIN_PACKS из виртуального модуля ~packs
  packs.d.ts            ← TypeScript-декларация для esbuild-плагина
  utils.ts              ← escHtml, sanitizeImageUrl
  modal.ts              ← showAlert / showConfirm (кастомные модалы)
  chessboard.ts         ← Chessground + chess.js (FEN-рендер и валидация)
  styles.css            ← кастомные стили (доска, карточки, координаты)
  screens/
    home.ts             ← главная: новая игра, продолжить, редактор
    setup.ts            ← настройка: пак, игроки, таймер
    game-board.ts       ← игровое поле
    question.ts         ← экран вопроса: доска, таймер, кнопки игроков
    results.ts          ← итоги с рейтингом
    editor.ts           ← визуальный редактор наборов
build.js                ← esbuild + auto-packs плагин
```

## Формат JSON-набора

```json
{
  "title": "Название набора",
  "categories": [
    {
      "name": "Название категории",
      "questions": [
        {
          "value": 100,
          "question": "Текст вопроса",
          "answer": "Текст ответа",
          "image": "https://example.com/img.png",
          "fen": "rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1"
        }
      ]
    }
  ]
}
```

Поля `image` и `fen` — опциональные.

## Разработка

```bash
npm install
npm run build               # сборка
npm run build -- --watch    # watch-режим (пересборка при изменениях)
```

Открыть `index.html` в браузере (без сервера, через `file://`).

## Стек

- TypeScript + esbuild
- Chessground + chess.js
- Tailwind CSS (CDN)
- localStorage для хранения данных
