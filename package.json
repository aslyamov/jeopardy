{
  "name": "jeopardy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:js": "esbuild src/app.ts --bundle --outfile=dist/bundle.js --sourcemap --minify",
    "build:css": "tailwindcss -i src/input.css -o dist/style.css --minify",
    "build": "npm run build:js && npm run build:css",
    "dev:js": "esbuild src/app.ts --bundle --outfile=dist/bundle.js --sourcemap --watch",
    "dev:css": "tailwindcss -i src/input.css -o dist/style.css --watch",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.27.3",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.9.3"
  }
}
